Git:

1-
git --version
git config --global user.email "tknack66@gmail.com"
git config --global user.name "Tknack"
git config --list
git config user.name
git config user.email

2-
git init
git status

----------
Untracked Files:			Staged Changes				Commits
 myFile.js	--- git add ---------->  myFile.js	--- git commit --->	1ab49
----------

		
3- 
git add package.json
git add .
git commit -m "initial"
git status

4 -
si après un changement dans le fichier Login.js par exemple, on peut revenir en arrière par:

git checkout -- imports/ui/Login.js

pour tout rétablir par rapport au dernier commit:

git checkout -- .

5-
git add .
git reset HEAD . // annule le dernier git add

6-
git remote add origin git@github.com:tknack/short-lnk-meteor.git
git remote
git remote -v
git push -u origin master


Github:
créer une clé rsa pour github
ssh-keygen -t rsa -b 4096 -C "tknack6@gmail.com"

obtenir la clé de l'gent ssh
 eval "$(ssh-agent -s)"

obtention de la clé publique locale
ssh-add ~/.ssh/id_rsa

afficher la clé rsa à coller dans github
less ~/.ssh/id_rsa.pub
(rubrique SSH and GPG Keys -> new SSH Key)

Accéder à github
ssh -T git@github.com
